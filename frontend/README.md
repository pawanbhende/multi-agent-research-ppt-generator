# Research Deck Studio Frontend

Static frontend for the Multi-Agent AI Research & Presentation Generator.

## Run

```bash
cd frontend
npm start
```

Open `http://localhost:3000`.

The app expects the FastAPI backend at `http://localhost:8000/api/v1`.
